.container.mt-4
  - flash.each do |name, message|
    .alert.alert-dismissible.fade.show{ class: "alert-#{ name == 'notice' ? 'success' : 'danger' }", role: 'alert'}
      = message
      %button.btn-close{ type: 'button', data: {bs_dismiss: 'alert' }, aria: { label: 'Close' }}